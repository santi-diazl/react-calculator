import styled from 'styled-components';

export const Wrapper = styled.div`
  background-color: var(--bgColour);
  text-align: right;
  color: white;
  font-size: 2em;
  padding: 10px;
  flex-grow: 1.5;
`;
